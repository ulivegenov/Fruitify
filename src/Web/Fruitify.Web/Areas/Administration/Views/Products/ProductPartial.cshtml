@using Fruitify.Common

@{
    ViewData["Title"] = "Product view";
}

@for (int i = 0; i < @Model.Entities.Count; i++)
{

    <tr onclick="location.href = '@Url.Action("Details", new { id = Model.Entities[i].Id })'">
        <th class="align-middle" scope="row">@((i + 1) + (Model.CurrentPage - 1) * GlobalConstants.ItemsPerPageAdmin )</th>
        <td class="align-middle">@Model.Entities[i].Name</td>
        <td class="align-middle">@Model.Entities[i].Price.ToString("F2")</td>
        <td class="align-middle">@Model.Entities[i].PromoPrice.ToString("F2")</td>
        @if (Model.Entities[i].DayProduct)
        {
            <td class="align-middle">ДА</td>
        }
        else
        {
            <td class="align-middle">НЕ</td>
        }
        @if (Model.Entities[i].WeekProduct)
        {
            <td class="align-middle">ДА</td>
        }
        else
        {
            <td class="align-middle">НЕ</td>
        }
    </tr>
}
